# Declarative Authority for AI Agents

> 본 명세는 계산의 정확성이 아니라 입력의 누락을 다룬다.
> LLM의 말을 믿지 말고, 검증 절차의 완수 여부만 코드로 세라

*RFC: Separating the authority to declare, to certify, and to execute*

---

## 0. 기존 접근과의 차이

기존 논의는 "LLM의 정확도를 올리고, 스키마 및 하드코딩 가드레일로 입력 인자를 검증하는 방식"이다. 본 명세는 "LLM에게 무엇을 검증하고 물어볼지 정하는 권한을 회수하고, 외부 체크리스트 항목의 확인 여부를 코드가 세어 판정 기록으로 남기는 방식"을 제안한다.

| 축 | 기존 | 본 명세 |
| --- | --- | --- |
| **규칙을 누가 정하는가** | 아무도 정하지 않아 모델이 호출마다 정한다. 무엇을 묻고 무엇을 건너뛸지가 그때그때 갈리고, 규칙으로 남지 않아 열거할 수 없다(2.3). | 정의 권한을 가진 제공자·사용자·적용처가 외부 체크리스트에 선언한다. 배포 없이 바뀌고 위치가 고정되어 열거된다. |
| **무엇을 확인 대상으로 두는가** | 인자만이다. Structured Outputs / Tools 스키마는 데이터 타입과 형식(Syntax)을 볼 뿐이고, 조건과 의도는 적을 필드가 없다(2.4). | 값·조건(잔액·권한)·의도를 동일 선상의 슬롯으로 둔다. 판정 축이 행위이므로 조건 전용 장치가 따로 필요 없고, 확신도 점수는 쓰지 않는다(3.3). |
| **빠진 규칙이 드러나는가** | 선언되지 않은 항목은 기록에도 없어, 누락이 아니라 처음부터 없던 항목으로 보인다(2.6). | 확인할 수 없는 항목은 UNKNOWN으로 남아 정의 보완으로 분기한다(4.4). 메우지는 못하고, 누가 무엇을 선언하지 않았는지가 기록에 남는다. |
| **채워진 값을 믿을 근거가 있는가** | 조회된 값과 발명된 값이 같은 문자열이라 사용자도 검증기도 구분하지 못한다(2.2). | 모델이 채워도 그것으로 슬롯이 확인되지 않는다. 지정된 소스를 모두 조회한 뒤에도 없으면 UNKNOWN으로 고정한다(5.2). |
| **채워진 것이 전부인지 누가 판정하는가** | LLM이 채워온 말끔한 서류를 사람이 통과 클릭(Click-through)만 한다. 확신도 점수는 모델의 자기 평가다. | 계수하는 코드가 한다. 사람에게는 채워진 서류가 아니라 빈칸이 가고, 사람만 답할 수 있는 항목만 돌아간다(3.5). |
| **실행은 누가 결정하는가** | 검증이 실행 흐름 안의 분기라 건너뛸 수 있고, 그 사실도 남지 않는다. | 판정은 기록으로 끝나고, 외부 실행 주체가 그 기록을 조회해 스스로 결정한다. 기록을 거치지 않는 실행 경로가 없다. |

LLM의 출력은 실행의 근거가 아니라 실행 준비를 위한 입력이다. 모델의 일이 줄지는 않는다. 값 추출, 대화, 도구 후보 매칭은 그대로 모델에 남는다. 옮기는 것은 권한 둘이다. 무엇을 검증할지 정하는 권한(정의 권한)은 체크리스트를 선언하는 주체에게, 채워진 슬롯이 실행해도 좋을 만큼 완전한지 판단하는 권한(인증 권한)은 계수하는 코드에 둔다. 본 명세에서 인증은 이 완전성 판정을 가리키며, 준비 작업 전체를 가리키지 않는다.

### 0.1 기존 자산과의 관계

- 신규 아키텍처 도입이 아니다. 현재 존재하는 두 자산인 도구 목록(Tool List), 입력 스키마(Input Schema)에 항목을 더하는 확장이다.
- 형식 검증기와 도구 서버의 검증은 대체되지 않는다. 본 명세는 판정 기록을 생성할 뿐이며, 두 검증은 실행 경로에 그대로 남는다.
- 사후 검증 계층을 대체하지 않는다. 본 명세는 실행 전 값·조건·의도가 선언된 소스에서 확인되었는지 세어 실행 가능한 상태인지 결정론적으로 판정한다(3.5). 이는 차단이 목적이 아니라 실행 가능한 상태에 도달하기 위한 과정이다. 판정 기록은 실행과 후속 검증의 근거가 된다.

---

## 1. 적용 범위 (Scope)

본 명세는 전체 툴 호출에 일괄 적용하는 것이 아니다. 적용 대상과 슬롯 구성은 도메인별로 축소된다.

### 1.1 적용 대상

- 적용 — 비가역적(Irreversible) 실행. 결제, 발주, 전송, 삭제, 물리 제어.
- 비적용 — 조회(Read-only) 호출은 게이트 대상이 아니다.
- 교환 조건 — 답변 품질에 대한 진술을 판정 근거에서 떼어내는 대신 입력 완전성을 얻는다. 되돌릴 수 있는 실행에서는 이 맞바꿈의 이득이 비용을 넘지 않는다. 결과를 보고 고치는 편이 더 싼 통제 수단이다.

### 1.2 슬롯 축소 규칙

- 즉시 실행만 존재하는 도메인 — 시점(Trigger) 슬롯 불요.
- 도구가 단일한 시스템 — 의도 슬롯의 도구 매칭 항목 불요.
- 에이전트와 도구의 소유자가 동일한 경우 — 자체 체크리스트는 여전히 필요하며, 도구별 체크리스트는 입력 스키마 자리에 둔다.

---

## 2. 시스템 문제 정의

전제 — LLM은 빈칸을 만나면 채우는 엔진이다. 이는 결함이 아니라 훈련된 동작이므로 금지 지시나 성능 향상으로 제거되지 않는다. 아래 항목은 이 성질이 시스템 구성과 만나 생기는 고장이다.

### 2.1 연산 추론과 실행 권한의 혼동

- 현재 시스템은 확률적 텍스트 생성으로 추출된 인자를 API 실행 권한과 동일시한다. 확신도가 높아도 추론은 자원을 변경하거나 비가역적 행위를 수행할 권한을 만들지 못한다.
- 확신도 점수는 예측이 틀릴 확률을 재는 도구다. 값을 조회했는지 지어냈는지는 확률이 아니라 사실이어서, 점수를 매기면 지어낸 값에도 높은 점수가 붙는다. 점수가 부정확해서가 아니라 재는 것이 달라서 쓰지 않는다. 대신 재는 것은 3.2에 있다.
- 권한은 정확도가 아니라 소유에서 나온다. 적을 수 있다는 것과 책임진다는 것은 한 쌍이며, 책임지지 않는 모델은 완전하다고 인증할 수도 없다.

### 2.2 1차 작성 주체의 이관과 자동화 편향

- 기존 시스템에서 사용자는 폼 작성과 승인 검수를 모두 직접 수행했다. 에이전트 구조에서는 1차 작성 주체가 LLM으로 이관된다.
- 파라미터가 정교해질수록 사용자는 판단을 생략하고 절차만 통과시킨다. 정확도 향상은 이 취약점을 악화시킨다.
- 조회된 값과 발명된 값은 같은 문자열 형태를 가져 사용자도 구분하지 못한다.

### 2.3 되묻기와 사후 배제가 성립하지 않는 이유

- 공통 근거 — 무엇을 물을지, 무엇을 제외할지 결정하는 주체가 LLM 자신이다. 자기 출력을 사후에 분류하라는 요구는 다시 추론이 된다.
- 되묻기 — "모호하면 질문하라"는 지시는 작동하지 않는다. 누락을 메타인지하지 못하거나 환각으로 빈칸을 채운 경우 질문 자체가 생성되지 않는다.
- 사후 배제 — "추론한 값은 제외하고 찾아라"는 수행 불가능하다. 두 값의 형태가 같아(2.2) 구분 기준이 없다.
- 질문을 생략한 출력에는 생략했다는 표시가 남지 않는다. 누락은 실행 이후에만 드러난다.

### 2.4 조건 선언 위치의 부재

- 입력 스키마는 호출을 조립하는 형식이라 인자만 필드를 갖고, 조건을 적을 필드가 없다.
- 연쇄 결과 — 적을 자리가 없다 → 검증 대상이 아니다 → 아무도 선언하지 않는다 → UI에 표시되지 않아 사용자는 보지 않은 항목까지 승인한다 → 호출 시점에 모델이 채운다.
- 현행 검증기는 스키마 부합만 본다. 값의 실재는 대조할 대상이 없어서, 조건의 충족은 적을 칸이 없어서 보지 않는다.
- 인자가 모두 존재하고 타입도 유효하지만 실행하면 안 되는 경우가 이 영역이다. 예: 잔액 부족, 수신자 부재, 시점 미확정, 권한 없음.
- 도구 서버가 조건을 검증하기도 한다. 다만 호출 이후의 검증이어서, 실행 전에 조건을 확인했다면 나가지 않았을 호출이 이미 나간 경우가 생긴다. 거절 사유는 판정 기록으로 남지 않고, 모델이 값을 바꿔 재시도하는 단서가 될 수 있다.
- 양쪽의 조건이 서로 전달되지 않는다. 제공자의 조건은 실행 전에 확인할 수 있는 형태로 에이전트에 닿지 않고, 사용자가 정한 조건은 도구 서버에 닿지 않는다.
- Description에 자유 텍스트로 적힌 조건도 모델이 인지했는지 확인할 방법이 없어 검증되지 않는다.

### 2.5 실패 유형 분류

실패는 서로 다른 고장이 아니라 무엇이 추론으로 채워졌는지의 차이다. 세 경우 모두 선언된 소스 없이 모델이 채운 자리가 실행에 도달했다. 같은 결과도 추론된 자리가 다르면 귀속처가 다르다(7.1).

- 값이 추론되었을 때 (→ 잘못된 실행) — 값을 조회할 소스가 지정되지 않아 호출 시점에 모델이 채운다.
- 조건이 추론되었을 때 (→ 지시 없는 실행) — 조건이 선언되지 않아 충족 여부를 모델이 가정한다.
- 의도가 추론되었을 때 — 의도와 허용 범위가 선언되지 않아 대조할 기준이 없다.
  - 무엇을 할지(도구와 상태 변화)가 추론되면 (→ 다른 실행) — 체크리스트를 확정하는 선행 항목(4.1)이 틀어져, 도구 선택이 갈리고 확인할 값과 조건도 함께 달라진다.
  - 허용 범위나 실행 시점(언제, 어떤 사건에 실행할지)이 추론되면 (→ 지시 없는 실행) — 하려는 행위는 맞아도 사용자가 허락하지 않은 범위나 시점에 실행된다. 예: 예약이 마감되자 기존 예약을 취소하고 예약함, "가격이 떨어지면 사 줘"의 기준을 모델이 정해 사용자가 예상하지 못한 순간에 매수함.

빈칸의 상당수는 사용자가 말하지 않은 데서 시작한다. 사람은 자기가 무엇을 빠뜨렸는지 대개 모르므로 불완전한 지시는 통상적인 상태다. 문제는 모델이 빈칸을 채워 그 사실이 드러나지 않고, 다음 지시도 같은 빈칸을 안은 채 반복되는 것이다.

부족한 입력을 추론으로 채운 출력은 옳고 그름을 판정할 대상이 아니다. 이번에 맞았다는 사실이 다음에도 맞는다는 근거가 되지 않는다.

### 2.6 판정 기록의 부재

실행 기록은 남지만 판정 기록은 남지 않는다. 기록되는 것은 호출된 도구와 최종 인자값이며, 그 값이 어디서 왔는지와 어떤 조건이 확인되었는지는 기록 대상이 아니다.

- 값에 출처가 부착되지 않아, 2.3의 관측 불가능성이 실행 이후 분석까지 이어진다.
- 선언되지 않은 조건은 기록에도 없어, 확인하지 않은 항목이 누락이 아니라 처음부터 없던 항목으로 보인다.
- 차단된 실행은 기록되지 않는다. 실행된 건만 남으면 무엇이 걸러졌는지가 소실된다.
- 승인 기록은 승인 사실만 남기고 제시된 내용을 남기지 않아, 사고 시 승인자가 무엇에 서명했는지 재구성할 수 없다.

귀결 — 원인 후보가 기록에 없으므로 이상은 남은 설명으로 귀속된다(7.1). 2.5의 세 유형이 구분되지 않아 제공자 미선언과 사용자 미지정도 에이전트나 모델의 오류로 집계되고, 개선 수단은 그 귀속처에 묶인다.

### 2.7 내부 규칙의 산재

에이전트 자체 규칙이 프롬프트, 도구 래퍼 분기, 문서화되지 않은 관행에 분산되어 있다. 문제는 산재 자체가 아니라 변경 비용과 확인 불가다.

- 규칙 하나를 변경하려면 코드 수정과 배포가 필요하다.
- 규칙이 여러 위치에 흩어져 있어 현재 적용 중인 규칙 전체를 열거할 수 없다.

---

## 3. 핵심 아키텍처 원칙

### 3.1 파이프라인

```
[기존 구조]
LLM Output ─> [Schema Syntax Check] ─> API Execution
                                        (위험: 미선언 조건의 임의 추정)

[제안 구조]
LLM Output ─> [External Declarative Slots] ─> [Deterministic Gate Counter]
                        │                              │
                        ├─ Source Verification         ├─ Unknown Count == 0
                        └─ Authority Check             └─ KNOWN / NULL / UNKNOWN
                                                       │
                                                       ▼
                                               [Decision Record]
                                               unknown_count · unmet
                                                       │
              ┌────────────────────────────────────────┘
              │  (실행 주체가 판정 기록을 조회)
              ▼
              [Execution Path] ─> [Schema Syntax Check] ─> API Execution
                                       (기존 검증기 유지)
```

### 3.2 판정 축의 전환 (답변의 옳음 → 실행의 정확성)

- 비가역적 실행에서 정확성의 대상은 생성된 문장이 아니라 수행된 행위다. 행위가 정확하다는 것은 사용자가 의도한 그 행위이고 제공자가 요구한 조건을 충족한다는 뜻이다.
- 본 명세가 판정하는 것은 그 정확성의 전제인 입력 완전성이다. 값·조건·의도가 모두 선언되고 각 슬롯이 지정된 소스에서 확인되어야 정확성을 따질 수 있다. 확인된 요건의 충족 여부는 따로 기록된다(6.3).
- 판정 축이 생성된 답변에서 행위로 옮겨지면 세 가지가 따라 나온다. 행위는 확률이 아니라 사실이므로 점수가 아니라 계수로 판정된다. 값·조건·의도의 미확인은 모두 "확인 없이 진행했다"는 하나의 행위이므로 한 카운터로 센다(3.3). 그리고 행위는 하기 전에 결정되므로 실행 전에 판정할 수 있다.

### 3.3 동일 선상의 슬롯 통합 (Value, Condition, Intent)

- 값(Value), 조건(Condition), 의도(Intent)를 검증 슬롯으로 취급한다. 값은 도구 인자에 들어갈 값, 조건은 실행 전에 성립 여부를 확인할 사실(잔액·권한 등), 의도는 사용자가 원하는 실행 단위와 그 안의 허용 범위다(4.1, 4.3). 판정 축이 행위이므로(3.2) 조건 전용 장치가 따로 필요 없다. 계수 방식은 동일하고, 확정 순서에서는 의도가 앞선다.
- 판정이 실행과 분리되면 "조건을 확인하지 않았다"는 슬롯 상태가 존재할 수 있다. 그때 비로소 자유 텍스트 조건도 검증 대상이 된다.

### 3.4 결정론적 슬롯 카운터 게이트

- 판정은 LLM의 추론이나 점수 계산을 거치지 않는다.
- 외부 검증기가 슬롯 상태를 집계하여 Unknown Count == 0이면 확인 완료로 판정한다. 확인 완료는 충족을 뜻하지 않는다(6.3).

### 3.5 계산 목표의 전환 (실행 여부 → 미확인 슬롯)

- 계산의 목표는 실행 여부가 아니라 미확인 슬롯의 확정이다. 실행 여부는 그 결과에서 파생된다.
- 사람이 받는 것은 완성된 서류가 아니라 빈칸들이다. 빈칸은 유한하고 각 항목에 출처가 붙으며, 사람은 자기만 답할 수 있는 항목에 답한다. 2.2의 통과 클릭은 검토 대상이 이미 채워져 있어 성립했으므로, 빈칸들에서는 성립하지 않는다.
- 판정 기록이 있고 미확인 슬롯과 미충족 항목이 모두 없는 상태를 실행 가능한 상태라 한다. 그 밖의 상태에서는 실행의 근거가 없다.
- 게이트는 실행 가능한 상태인지를 확정해 기록할 뿐 실행 여부를 지시하지 않는다. 본 명세는 판정까지 다루며 실행을 포함하지 않는다. 실행 주체는 이 로직의 하위 단계가 아니라 완전히 외부에 있고, 판정 기록만 참조해 실행 여부를 스스로 결정한다.
- 판정 함수의 반환값을 받아 분기하는 구조는 분리가 아니다. 기록을 거치지 않는 실행 경로가 존재하지 않아야 한다.

---

## 4. 선언적 슬롯 체계

모든 값·조건·의도 항목은 정의 권한을 가진 주체에 따라 분리된 외부 체크리스트에 등록된다. 누가 정의하는가와 무엇이 해소하는가는 다른 축이다. 제공자가 선언한 조건이 시스템 측정으로 해소되기도 한다. 4절은 정의 주체를, 5절은 해소 소스를 다룬다.

```
                  ┌─────────────────────────────────────────┐
                  │      Global Fixed Checklist             │
                  │  - Intent (incl. Tool Match) / Trigger  │
                  └───────────────────┬─────────────────────┘
            ┌──────────────────────────┴──────────────────────────┐
            ▼                                                     ▼
┌───────────────────────────────────────┐ ┌───────────────────────────────────────┐
│       Provider Checklist              │ │         User Checklist                │
│ - Schema / Type Validation            │ │ - User Intent Bounds / Context        │
│ - Mandatory Pre-conditions (Balance)  │ │ - Execution Limits & Scope            │
│ - State Changes & Action Labels       │ │ - User Preference / Approval          │
└───────────────────────────────────────┘ └───────────────────────────────────────┘
```

### 4.1 고정 체크리스트 (Fixed Checklist) - 실행 단위를 확정한다. 모든 툴 호출에 공통 적용.

- 도구 매칭 유효성 — 요청된 상태 변화를 해당 도구가 유효하게 제공하는가.
- 실행 시점 — 즉시 실행인가, 특정 시간이나 특정 사건 발생 시 실행인가.
- 의도 — 사용 목적과 원하는 상태 변화를 제시.

**고정 체크리스트의 역할: 실행 단위 확정에 필요한 최소치다. 이것이 정해져야 도구 매칭이 결정되므로 4.3의 의도보다 선행한다. 실행 시점 항목은 지시 시점에 확정되며, 트리거 시점에 확인하는 조건이 아니다.**

### 4.2 제공자 체크리스트 (Provider Checklist) – 도구마다 다르다.

- 필수 인자 및 타입/형식.
- 실행 금지/허용 조건 — 소유권 확인, 접근 권한, 실행/종료 전 확인 항목, 안전, 허용된 실행 방식(분할 실행 허용 여부 등) 등.
- 실행 시 발생하는 상태 변화 (State Change) 선언.

#### 4.2.1 도구 설명 메타데이터 라벨 (Description Labels)

제공자는 이미 Tool Description에 조건을 기술하고 있다. description은 도구 목록 수신 시점에 확보되므로 문자열 파싱만으로 슬롯이 된다. 다만 필수 여부가 합의되지 않아 강제 체크리스트가 되지 않고 있다. 다음 세 라벨을 제안한다.

- [Required] — 인자에 붙는다. 외부 소스를 통해 소스 위치가 대조되어야 하는 필드.
- [Verify] — 조건에 붙는다. 사용자 승인 슬롯을 생성한다.
- [Notice] — 제약 사항에 붙는다. 실행 전 사용자에게 사전 안내되어야 한다.

조건에 따라 검증 수단(사용자 승인인지, 어느 시스템 상태를 조회해 대조하는지)까지 함께 선언할 수 있다.

라벨을 강제 체크리스트로 삼으려면 파싱 계약이 게이트의 일부가 된다. 선언 주체가 신뢰되고, 구문이 고정되어 있고, 파싱이 결정론적이어야 한다. 파싱에 실패한 항목은 통과가 아니라 UNKNOWN으로 둔다.

제공자가 선언한 조건은 실행하지 않을 근거로만 쓴다. 조건을 통과했다는 사실이 실행을 정당화하지는 않는다.

### 4.3 사용자 체크리스트 (User Checklist) – 사용자/환경마다 다르다.

- 의도와 맥락 구체화.
- 실행 한도/범위 제어, 실행/종료 전 확인 항목, 안전, 사용자 선호 등.

**사용자 체크리스트의 역할: 확정된 실행 단위 안에서의 허용 범위를 정한다. 단위가 정해진 뒤에 채워지므로 4.1의 의도보다 후행한다.**

### 4.4 미확인 상태의 해소 경로

항목은 같은 방식으로 계수되지만 미확인 시 해소 경로가 다르다. 구현 시 분기가 필요하다.

- 값은 대체로 사용자가 답할 수 있고, 조건은 그렇지 않은 경우가 많다. 다만 실제 분기는 값·조건 축이 아니라 누가 해소할 수 있는가로 갈린다.
- 해소 경로는 셋이다. 경로는 슬롯에 선언된 허용 소스와 조회 결과로 정해지며, 모델이 고르지 않는다.
  - (a) 사용자 질의 (ask_user) — 사용자가 답할 수 있는 항목.
  - (b) 시스템 조회 (system_lookup) — 시스템만 답할 수 있는 항목. 사용자에게 되묻지 않는다.
  - (c) 정의 보완 (definition_repair) — 조건 자체가 선언되지 않았거나 미들웨어가 해석할 수 없는 경우. 사용자 응답으로 메워지지 않으며 제공자 또는 구현 측의 갭이다. 조회 훅이 실패한 건도 여기에 속한다.
- 보류(hold)는 해소 경로가 아니다. 조건이 확인되었고 그 조건이 실행을 금지하는 경우로, 슬롯은 KNOWN이고 질의로 돌아가지 않으며, 미충족 항목으로 기록된다(6.3). 보류는 끝난 확인(잔액 부족)이고 정의 보완은 빠진 확인(선언되지 않은 조건)이다. 둘을 "해소할 수 없음"으로 합치면 과제가 어디로 가야 하는지가 사라진다.
- 사용자 질의 경로에는 상한이 있다. 상한에 닿으면 슬롯은 UNKNOWN인 채로 남고, 해소는 이 로직 바깥의 대화로 넘어가며, 상한 도달 자체가 판정 기록에 남는다. 상한의 값은 적용처가 정한다. 상한이 없으면 답할 수 없는 항목이 무한히 재질의되고, 그 기록은 사용자의 포기(6.4)와 구분되지 않는다.
- 값은 지시 시점에, 조건은 트리거 시점에 확인한다.

---

## 5. 화이트리스트 탐색 파이프라인

### 5.1 3-State 슬롯 상태 모델

- KNOWN — 화이트리스트 탐색으로 데이터 출처가 확인되었거나 승인이 완료된 상태.
- NULL (결과 없음) — 조회했으며 “없음”을 확인함.
- UNKNOWN — 필요한 확인이 아직 끝나지 않은 상태. 모델의 자기 보고가 아니라 탐색이 끝난 뒤 남은 결과다.
- UNKNOWN은 질문 대상과 같은 말이 아니다. 사용자 질의는 4.4의 세 경로 중 하나일 뿐이다.
- NULL 상태를 별도로 두는 근거 — 지어내기를 막는 수단은 금지가 아니라 "없음"을 기입할 수 있는 정상 선택지를 제공하는 것이다.
- 세 상태가 구분하는 것은 확인 여부다. 충족 여부는 상태가 아니며, 미충족 항목으로 따로 기록된다(6.3).

### 5.2 탐색 순서 (Lookup Cascade)

아래는 신뢰 순위(Trust Tier)가 아니라 조회 순서(Search Order)다. 앞선 소스에서 값이 확인되면 그 지점에서 조회를 종료하며, 소스별 신뢰도 가중치는 계산하지 않는다.

user_answer → instruction → pre_set_data → measured_data → prior_state

- user_answer — ask_user 인터랙션 이후 사용자가 직접 입력한 응답 데이터. 첫 회차에는 없음.
- instruction — 사용자 발화로 확인된 구간에서 위치를 지정해 뽑아낸 값.
- pre_set_data — 사용자가 미리 승인해 저장해 둔 설정값.
- measured_data — 시스템 API를 조회해 그 자리에서 확인한 시스템 상태. 조회는 실측이어야 한다.
- prior_state — 이전 실행의 결과로부터 승계된 값. 값에만 적용되며 조건은 승계하지 않는다.
- 최종 단계까지 값이 확인되지 않으면 슬롯은 UNKNOWN으로 고정된다. 모델이 값을 제시하더라도 그것은 위치에 대한 주장일 뿐이고, 그 위치에 값이 실제로 있는지는 게이트가 대조한다. 모델의 정직함은 이 구조의 전제가 아니다.

### 5.3 instruction의 범위 정의

- 사용자는 모호하게 시작하고 수 회의 왕복을 거쳐 도구를 확정한다. 따라서 instruction의 범위는 마지막 프롬프트 하나가 아니라, 도구 사용이 확정되기까지 이어진 대화 세그먼트 전체다.
- 범위를 단일 프롬프트로 구현하면 N턴 전에 언급된 값(예: 계좌번호)이 어느 소스에서도 조회되지 않아 불필요한 재질의가 발생한다.

---

## 6. 구현 체계

### 6.1 체크리스트·원칙·코드의 분할 기준

- 체크리스트에 두는 것: 소유 주체가 정하는 항목. 도구마다, 사용자마다, 규제마다 달라지며 배포 없이 변경된다.
- 원칙(Prompt)에 두는 것: 위반해도 코드가 탐지할 수 있는 항목. 프롬프트는 게이트에 걸리는 횟수를 줄이는 유도 수단이며, 실패해도 판정에서 걸러진다. 위반이 외부에서 관측되지 않는 항목은 프롬프트에 둘 수 없고, 슬롯으로 만들어 소스와 슬롯 상태가 생겨야 관측 대상이 된다.
- 코드(Gate)에 두는 것: 주체와 무관하게 동일한 절차. 계수, 대조, 기록이다. 체크리스트가 늘어도 이 절차는 변하지 않는다.
- 프롬프트는 대화가 길어지면 희석되고 누락 여부가 확인되지 않는다. 원칙은 도구 관련 대화마다 매 회 반영한다.

### 6.2 원칙 (Principles)

아래 여섯 항목은 앞 절의 규범을 프롬프트 문장으로 옮긴 예시다. 새 규칙이 아니므로 위반은 판정 기록에서 탐지되며, 추가·수정할 수 있다.

- 슬롯은 체크리스트에서, 값은 조회에서, 없는 것은 사용자와 관측에서 가져온다. 조립하지 않는다.
- 시점이 불분명하면 즉시 실행으로 기본값을 두지 않고 질의한다.
- 값은 지시 시점에, 조건은 트리거 시점에 확인한다.
- 값을 생성하지 않고 지정된 순서로 탐색한다. 값이 아니라 위치를 가리킨다.
- 재사용 가능한 값은 사용자의 동의를 받아 기록한다.
- 기록된 값은 재질의하지 않고 조회한다. 모델 컨텍스트의 기억에서 꺼내지 않는다.

아래 두 항목은 위와 성격이 다르다. 도구 선택 영역이므로 위반이 코드로 탐지되지 않으며(8.1), 프롬프트로만 완화된다.

- 언제 하라고 했는지, 무엇이라 불렀는지를 인지한 후 도구를 결정한다.
- 후보가 둘 이상이면 도구 이름을 노출하지 않고 행동을 되묻는다.

### 6.3 코드 게이트의 책임

- 체크리스트를 구성하고 UNKNOWN 슬롯 수를 센다. Unknown Count == 0 이면 확인 완료로 판정한다. 세는 것은 확인 여부이지 충족 여부가 아니다.
- 확인된 슬롯을 선언된 요건과 대조해, 요건을 채우지 못한 항목을 미충족 항목으로 기록한다. 요건은 체크리스트가 슬롯에 선언한 충족 기준이다(값은 필수 여부, 조건은 금지·허용 기준). 확인된 조건이 실행을 금지하는 경우(보류)와 필수 값이 NULL로 확인된 경우가 여기에 든다. 미충족 항목은 계수에 들어가지 않는다. 대조는 결정론적이어야 하며, 대조할 수 없는 조건은 UNKNOWN으로 두고 정의 보완으로 분기한다.
- 5.2의 탐색 순서를 순회하고, 슬롯이 가리킨 위치에 값이 실제로 있는지 대조한다. 순회와 대조의 주체는 게이트이며 모델이 아니다.
- UNKNOWN 슬롯을 4.4의 세 경로로 분기하고, 어느 경로를 탔는지를 판정 기록의 슬롯에 남긴다.
- 라벨을 읽어 슬롯으로 전개하고 판정에 반영한다.
- 승인은 {slot_id, value} 쌍에 결속하며 값 변경 시 무효화하고, 승인 기록에는 함께 제시된 슬롯 집합을 남긴다. 제시되지 않은 항목은 승인 범위에 포함되지 않는다. 2.2의 통과 클릭이 승인으로 성립하지 않게 하기 위함이다(2.6).
- 판정을 기록한다. 게이트의 산출물은 판정 기록이며 호출·차단·중단은 게이트의 동작이 아니다(3.5).

### 6.4 판정 기록과 실행 기록

- 기록은 다음 실행의 유일한 근거다. 실행 기록의 execution_decision이 "executed"인 건만 이후 호출에서 prior_state 소스로 인정된다.
- 실행되지 않은 건도 기록한다(2.6). 반복 질의와 사용자의 포기는 여기서만 드러난다.
- 조회나 검증이 실패하면 통과가 아니라 UNKNOWN으로 남는다. 어느 슬롯에서 무엇이 실패했는지가 함께 기록되며, 실패를 우회하는 경로가 없다.
- 동일 실행 단위의 기록은 같은 키 아래 누적된다. 지시 시점과 트리거 시점이 함께 남아야 그사이 시스템 상태가 어떻게 바뀌었는지 확인할 수 있다.

필드 이름과 구성은 적용처에서 정한다. 판정 기록에 남아야 하는 것은 슬롯의 상태·출처·종류, UNKNOWN의 분기 경로, unknown_count, 미충족 항목이다. unknown_count는 slots와 user_checklist를 함께 세며, 배열이 나뉘어도 계수는 하나다. 판정 기록은 실행 여부를 담지 않는다.

```json
{
  "action_key": "u_1042:transfer_funds",
  "phase": "at_trigger",
  "tool_name": "transfer_funds",
  "slots": [
    {"slot_id": "recipient_account",     "status": "known",   "source": "user_answer"},
    {"slot_id": "amount",                "status": "known",   "source": "instruction"},
    {"slot_id": "daily_limit_check",     "status": "known",   "source": "measured_data"},
    {"slot_id": "account_balance_check", "status": "unknown", "source": null,
     "allowed_sources": ["measured_data"], "lookup_failed": true,
     "route": "definition_repair"}
  ],
  "user_checklist": [
    {"item_id": "confirm_recipient", "status": "known",    "source": "user_answer"}
  ],
  "unknown_count": 1,
  "unmet": [
    {"slot_id": "daily_limit_check", "reason": "prohibited"}
  ],
  "timestamp": "2026-03-11T09:24:07Z"
}
```

daily_limit_check는 확인되었으나 요건을 채우지 못해 미충족 항목에 남고, account_balance_check는 조회 훅이 실패해 정의 보완으로 분기했다. 실행 주체는 이 기록을 조회한 뒤 자신의 결정을 별도 기록으로 남긴다.

```json
{
  "action_key": "u_1042:transfer_funds",
  "ref_phase": "at_trigger",
  "actor": "executor",
  "execution_decision": "not_executed",
  "timestamp": "2026-03-11T09:24:09Z"
}
```

물을 항목도 실행할 근거도 없으므로 실행하지 않았다. execution_decision의 값 체계는 적용처가 정하며, 본 명세가 의미를 부여하는 값은 prior_state 승계 조건인 "executed" 하나다(5.2).

---

## 7. 운영 관점의 귀결

### 7.1 감사 추적성 및 책임 소재 분리

빈 슬롯이 어느 체크리스트의 항목인지가 곧 과제의 귀속처다.

| 추론된 것 | 선언되지 않은 것 | 귀속처 |
| --- | --- | --- |
| **값** | 값을 조회할 소스가 지정되지 않음 | 백엔드 API/DB 연동 점검 |
| **조건** | 조건이 선언되지 않음 | OpenAPI Specification / Description 메타데이터 보완 |
| **의도** | 의도와 허용 범위가 선언되지 않음 | UX/대화 흐름 설계 개선. 도구 선택 오류도 여기에 귀속된다. |

- 세 유형 모두 모델 학습 예산을 요구하지 않는다. 개선 속도가 아니라 개선의 종류가 바뀐다.
- 현재 에이전트의 이상 현상은 조직 안에서는 에이전트 개발자에게, 밖에서는 모델에 귀속된다. 어느 쪽도 규명의 결과가 아니라 기록이 없어서 남은 설명이며, 개선 수단도 각각 규칙 덧붙이기(2.7)와 모델 교체로 고정된다.
- 사용자가 말하지 않아서 값과 조건이 빈 경우의 귀속처는 질의 시점과 문구다. 조건이 선언되면 제공자에게 귀속되는 항목이 분리된다.
- 미들웨어가 조건을 해석하지 못해 4.4의 (c)로 분기한 건은 파서와 조회 경로의 문제이므로 구현 측에 귀속되며, 사용자나 제공자에게 이관되지 않는다.

### 7.2 도입 효과

- 조건이 검증 대상이 된다. 값만 보던 검증기가 조건까지 순회한다.
- 규칙이 데이터가 된다. 자체 규칙도 정책기관·회사·규제기관 규칙도 체크리스트에 한 줄로 등록하고 배포 없이 바꾸며, 체크리스트가 유한하고 위치가 고정되어 적용 중인 규칙을 열거할 수 있다.
- 확장 비용이 고정되고 모델 성능과 독립이다. 도구와 조건이 늘거나 LLM을 교체해도 게이트 코드는 변하지 않으며, 성능 향상분은 UNKNOWN을 채우는 대화 단계를 줄이는 효율로 흡수된다.
- 질문이 늘어나는 것이 아니라 드러난다. 값과 조건은 원래 사용자가 정할 항목이었고, 모델이 메워서 묻지 않았을 뿐이다. 시스템이 답할 수 있는 항목과 대화에서 이미 나온 값은 묻지 않고(4.4, 5.3) 반복되는 항목은 사전 설정으로 승격하므로, 절대량도 반드시 늘지는 않는다.
- 판정 기록이 모델 개발의 입력이 된다. 슬롯 단위로 슬롯 상태와 소스가 남아 실패 유형별 데이터가 생기고, 미선언·미지정 건이 분리되어 모델 오류로 남는 건이 실제 모델 문제에 가까워진다. 학습 입력은 값이 아니라 슬롯 상태와 소스이며, 마스킹 범위는 적용처가 정한다.

### 7.3 도입 시점 논거

- 미선언이 책임이 되고, 선언한 쪽에 이익이 생긴다. 이 압력이 없었던 것은 책임을 뒷받침할 기록이 없었기 때문이다. 선언한 제공자는 자기 책임이 아닌 건을 분리할 수 있고, 이 유인은 기록이 조직을 넘어 전달될 때 작동한다.
- 성능 향상으로는 해소되지 않는다. 사람의 지시 불완전과 선언되지 않은 조건은 다음 세대 모델로도 확보되지 않는다. 성능 향상은 모델이 할 수 있는 일을 넓히지만 사용자의 허용 범위를 알려주지 않으며, 능력이 커질수록 그 차이가 커진다.
- **모델이 대신할 수 없다. 어떤 도구가 무엇을 요구하는지, 어떤 실행이 비가역적인지는 그것을 소유한 주체만 안다.**
- 기다릴 이유가 없다. 새로운 기술 요소가 없고, 도구 하나에 적용해도 그 도구에 대해서는 즉시 성립한다.
- 비가역 영역은 확대된다. 로봇·자율주행에서 비가역성은 더 강하다. 지금의 체크리스트 작성은 선행 투자다.

---

## 8. 잔여 위험 및 한계

본 명세가 보장하는 것은 판정 없는 실행이 근거를 갖지 못하고 기록에서 식별된다는 것이다(10). 기록을 읽은 뒤의 실행은 명세 밖이며(3.5), 아래 항목은 이 보장의 범위 밖에 있다.

### 8.1 도구 선택 오류

- 도구 이름은 표식이며 능력과 별개다. 이름이 달라도 같은 상태 변화를 만들 수 있고, 이름이 맞아도 능력이 다를 수 있다.
- 도구 선택은 코드로 검증되지 않는다. 게이트는 확정된 체크리스트를 계수하므로 도구와 체크리스트를 정하는 단계의 오류는 계수로 검출되지 않는다. 되묻지 않고 단일 후보로 좁히면 도구 목록에 있다는 이유로 선택될 수 있고, 통과 여부는 선택된 도구의 입력 스키마로 결정된다.
- 완화 — 상태 변화 선언은 입력 스키마가 아닌 Description에 의존하므로, 이 지점은 의도 확인에 더 의존한다. 사용자가 해당 행동을 무엇이라 부르는지 기록해 두는 것이 보조 수단이다.

### 8.2 기록 위조와 훅 구현

- 판정과 실행의 분리는 판정 없는 실행을 근거 없는 실행으로 드러내는 것이며, 위조된 판정 기록에 근거한 실행은 드러내지 못한다.
- 훅의 구현은 본 명세가 규정하지 않는다. 훅이 모델을 호출해 판정하면 그 판정은 확률적이며, 게이트는 훅의 반환값을 대조하지 않는다.

### 8.3 요구와 조건의 충돌

제공자가 금지한 조건을 사용자가 해제하려는 경우가 있다. 본 명세는 이 충돌을 판정하지 않는다. 제공자 조건은 사용자 응답을 허용 소스로 갖지 않으므로 사용자의 요구가 대조 결과를 바꾸지 않는다. 조건은 확인된 채 미충족 항목으로 남고(보류), 누가 무엇을 선언했고 누가 무엇을 요구했는지가 기록된다.

그 요구를 허용할지는 규제기관의 규칙이나 적용처의 정책이 정하며, 본 명세는 판단에 필요한 기록을 제공한다. 합의가 바뀌면 바뀐 규칙이 체크리스트의 한 줄로 반영된다.

### 8.4 대안 검토

체크리스트를 프롬프트에 넣고 JSON으로 수신하는 방식은 물을 항목을 정하는 권한을 다시 모델에 반환한다. 슬롯이 비었다고 판단하는 주체가 모델이면 채웠다는 보고를 외부에서 검증할 수 없다. 본 명세가 요구하는 것은 체크리스트의 소재가 프롬프트 바깥이고 계수 주체가 코드라는 두 조건이다.

---

## 9. 선행 작업과의 관계

본 명세는 최근 프레임워크의 성과를 비판하거나 대체하지 않고 그 위에서 확장한다. 도구와 맥락을 프로토콜로 격리한 것(MCP), 출력을 스키마에 묶어 값과 출처를 함께 추출할 수 있게 된 것, 에이전트 오케스트레이션(LangGraph 등)이 그 성과다.

---

## 10. 결론

- 본 명세는 LLM의 환각을 제거하는 기술이 아니다. 모델의 출력은 그대로 실행 준비의 입력으로 남는다.
- 본 명세의 목표는 실행을 차단하는 것이 아니라, 실행에 필요한 것이 무엇인지 확인하고 실행 가능한 상태를 확정하는 것이다. 안전은 그 결과로 따라온다.
- 본 명세가 보장하는 것은 두 가지다. 실행 가능한 상태는 판정 기록으로만 확정되므로 판정 없는 실행은 근거를 갖지 못하고 기록에서 식별된다는 것, 그리고 사고 시 원인과 책임이 기록에 남는다는 것이다.
- 블랙박스가 전면 개방되지는 않는다. 개방되는 것은 판정에 무엇이 입력되고 무엇이 출력되었는가다. 사고 시 필요한 정보는 대개 모델의 내부 상태가 아니라 값의 출처이므로, 이 범위로도 질문의 상당 부분이 답해진다.

무엇을 검증할지 정하는 권한과 검증되었는지 판단하는 권한은 모델 바깥에 둔다. 문제가 생긴 것은 이 권한들이 모델에 있었기 때문이 아니라 어디에도 없었기 때문이다. 빈자리를 모델이 메워 왔다. 두 권한이 체크리스트와 코드에 자리를 잡으면, 모델이 채운 슬롯은 선언된 소스에서 확인될 때 실행 가능한 상태를 이룬다. 좁아진 것은 모델의 권한이고, 넓어지는 것은 활용 범위다.
